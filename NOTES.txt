senozoid.javul.eval.Evaluator
    Do not change the order of operator declarations. If the symbol of an operator is a substring of the symbol of another operator of same type (unary/binary), the smaller one must appear later in the checking order. For example, "<" should not be looked for before "<=", or ">" before ">=".

